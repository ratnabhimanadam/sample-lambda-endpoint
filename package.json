{
  "name": "sample-lambda-endpoint",
  "version": "1.0.0",
  "description": "Sample AWS Lambda API endpoint with SAM and CI/CD",
  "scripts": {
    "test": "jest",
    "build": "echo 'No build step required'",
    "deploy": "sam deploy --no-confirm-changeset --no-fail-on-empty-changeset"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
